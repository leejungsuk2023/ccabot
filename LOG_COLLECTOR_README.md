# Firebase Functions 로그 수집 도구

CareConnectBot 프로젝트의 Firebase Functions 로그를 분석하기 위해 전체 로그를 가져와서 루트 폴더에 저장하는 도구입니다.

## 📁 파일 구성

- `get-firebase-logs.js` - Node.js 스크립트 (메인)
- `get-firebase-logs.bat` - Windows 배치 파일
- `get-firebase-logs.ps1` - PowerShell 스크립트
- `LOG_COLLECTOR_README.md` - 이 문서

## 🚀 사용 방법

### 1. Windows 배치 파일 (권장)

가장 간단한 방법입니다. 더블클릭하거나 명령 프롬프트에서 실행하세요.

```cmd
get-firebase-logs.bat
```

### 2. PowerShell 스크립트

```powershell
# 기본 설정 (1000줄)
.\get-firebase-logs.ps1

# 사용자 정의 설정
.\get-firebase-logs.ps1 -Lines 2000 -Project "my-project-id"
.\get-firebase-logs.ps1 -Output "custom-logs.txt" -Lines 5000
```

### 3. Node.js 스크립트 직접 실행

```bash
# 기본 설정
node get-firebase-logs.js

# 옵션 설정
node get-firebase-logs.js --lines 2000 --project my-project-id
node get-firebase-logs.js --output custom-logs.txt --lines 5000
```

## ⚙️ 옵션 설명

| 옵션 | 설명 | 기본값 |
|------|------|--------|
| `--lines` / `-Lines` | 가져올 로그 라인 수 | 1000 |
| `--output` / `-Output` | 출력 파일명 | firebase-logs-YYYYMMDD-HHMMSS.txt |
| `--project` / `-Project` | Firebase 프로젝트 ID | 현재 설정된 프로젝트 |
| `--help` / `-Help` | 도움말 표시 | - |

## 📋 사전 요구사항

### 1. Node.js 설치
- [Node.js 공식 사이트](https://nodejs.org)에서 다운로드
- 버전 14 이상 권장

### 2. Firebase CLI 설치
```bash
npm install -g firebase-tools
```

### 3. Firebase 로그인
```bash
firebase login
```

### 4. 프로젝트 설정 (선택사항)
```bash
firebase use --add
```

## 📊 출력 파일 형식

생성되는 로그 파일은 다음과 같은 형식입니다:

```
# Firebase Functions 로그
# 수집 시간: 2024-01-15 14:30:25
# 프로젝트: my-project-id
# 라인 수: 1000
# 명령어: firebase functions:log --lines 1000 --project my-project-id
# ========================================

[실제 로그 내용...]
```

## 🔧 문제 해결

### ❌ "firebase: command not found"
Firebase CLI가 설치되지 않았습니다.
```bash
npm install -g firebase-tools
```

### ❌ "User not logged in"
Firebase에 로그인하지 않았습니다.
```bash
firebase login
```

### ❌ "No project active"
프로젝트가 설정되지 않았습니다.
```bash
firebase use --add
# 또는
node get-firebase-logs.js --project YOUR_PROJECT_ID
```

### ❌ "Permission denied"
프로젝트 접근 권한이 없습니다.
- Firebase 콘솔에서 프로젝트 권한 확인
- 올바른 Google 계정으로 로그인했는지 확인

## 💡 사용 팁

### 1. 대량의 로그 수집
```bash
node get-firebase-logs.js --lines 10000
```

### 2. 특정 프로젝트 로그 수집
```bash
node get-firebase-logs.js --project careconnectbot-prod
```

### 3. 정기적인 로그 백업
Windows 작업 스케줄러나 cron을 사용하여 정기적으로 실행할 수 있습니다.

### 4. 로그 파일 관리
- 생성된 로그 파일은 타임스탬프가 포함된 파일명을 가집니다
- 정기적으로 오래된 로그 파일을 정리하세요
- 큰 로그 파일은 압축하여 보관하세요

## 🔍 로그 분석 도구

수집된 로그를 분석하기 위한 추천 도구:

1. **VS Code** - 큰 파일도 빠르게 열고 검색 가능
2. **grep** (Linux/Mac) 또는 **findstr** (Windows) - 명령줄에서 패턴 검색
3. **LogViewer** - 전용 로그 뷰어 프로그램
4. **Excel/Google Sheets** - CSV 형태로 변환 후 분석

## 📈 성능 고려사항

- 많은 로그 라인을 요청할 경우 시간이 오래 걸릴 수 있습니다
- Firebase의 API 제한을 고려하여 적절한 간격을 두고 실행하세요
- 큰 로그 파일은 메모리 사용량이 많을 수 있습니다

## 🛠️ 커스터마이징

필요에 따라 스크립트를 수정할 수 있습니다:

1. **로그 필터링**: 특정 함수나 오류만 수집
2. **출력 형식**: JSON, CSV 등 다른 형식으로 변환
3. **자동화**: 정기적인 수집 및 알림 기능 추가
4. **클라우드 저장**: 수집된 로그를 클라우드 스토리지에 자동 업로드

## 📞 지원

문제가 발생하거나 기능 요청이 있으시면 프로젝트 관리자에게 문의하세요.

---

**CareConnectBot 프로젝트**  
Firebase Functions 로그 수집 도구 v1.0
